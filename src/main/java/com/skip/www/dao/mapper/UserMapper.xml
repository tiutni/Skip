<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.skip.www.dao.face.UserDao">

<select id="selectCntUser" resultType="int" parameterType="com.skip.www.dto.User">
	SELECT count(*)
	  FROM user_tb
	 WHERE user_id = #{userId}
	   AND user_pw = #{userPw}
</select>

<select id="selectUserInfoByUserno" resultType="int" parameterType="com.skip.www.dto.User">
	SELECT*FROM user_tb
	WHERE user_no #={userno}

</select>

</mapper>